One-line: the "Trusted by 500+ businesses" strip under the hero.

```jsx
<LogoWall names={["Intuit","Xero","FreshBooks","Gusto","Wave"]} />
```

If real logo files arrive, drop them into `assets/` and swap the name spans for images at the same muted opacity.
