One-line: the white circle holding the icon at the top of every service card.

```jsx
<IconCircle icon="receipt" />
<IconCircle icon="phone" size={44} />
```

Stroke width 1.75, never filled. On forest surfaces keep `tone="white"`; the pale tints are for light sections only.
