// The curved line-mesh that used to sit behind the dark sections is gone —
// dark surfaces are solid now, per the palette brief.
//
// The component is kept as a no-op so the marketing pages that have not been
// rebuilt yet keep compiling; it goes when the last of them is rewritten.
export default function HeroArt() {
  return null;
}
